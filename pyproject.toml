[project]
name = "ndc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Irzan Ahmad", email = "irzanahmad44@gmail.com" }
]
requires-python = ">=3.11.9"
dependencies = [
    "flask>=3.1.3",
]

[project.scripts]
ndc = "ndc:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
